// Prisma Schema - Generated Dynamically
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// This schema will be regenerated dynamically by the sync engine
// Initial empty schema for Railway deployment
